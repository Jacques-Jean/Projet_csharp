using System.Windows;

namespace Bibliotheque_classes // Must match the part before the dot in x:Class
{
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }

        private void InitializeComponent()
        {
            throw new NotImplementedException();
        }
    }
}